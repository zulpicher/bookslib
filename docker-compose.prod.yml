version: '3.8'

services:
  frontend:
    restart: always
    deploy:
      resources:
        limits:
          memory: 256M

  auth-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 256M

  books-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M

  reviews-service:
    restart: always
    deploy:
      resources:
        limits:
          memory: 256M

  db:
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
